{
  "name": "{{ project_name }}",
  "description": "{{ service_name }} MCP server",
  "transport": "sse",
  "url": "http://localhost:8001/sse",
  "auth": {
    "type": "bearer",
    "instructions": "Set MCP_BEARER_TOKEN in the Inspector to a valid OAuth access token."
  }
}
